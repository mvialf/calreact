/**
 * Valores por defecto utilizados en toda la aplicación
 * Centraliza constantes para cumplir principio DRY y CLAUDE.md
 */

// Valores financieros por defecto
export const DEFAULT_TAX_RATE = 19; // IVA estándar en Chile
export const DEFAULT_SUBTOTAL = 0;
export const DEFAULT_WINDOWS_COUNT = 0;
export const DEFAULT_SQUARE_METERS = 0;

// Valores geográficos por defecto
export const DEFAULT_REGION = 'RM'; // Región Metropolitana
export const DEFAULT_COUNTRY = 'Chile';

// Valores numéricos por defecto
export const DEFAULT_LATITUDE = 0;
export const DEFAULT_LONGITUDE = 0;

// Valores de cadena por defecto
export const DEFAULT_EMPTY_STRING = '';